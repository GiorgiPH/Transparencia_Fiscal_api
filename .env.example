# ============================================
# PLANTILLA DE CONFIGURACIÓN - TRANSPARENCIA FISCAL
# ============================================
# Copia este archivo como .env, .env.development, .env.production o .env.test
# según el entorno que necesites configurar.
# ============================================

# ============================================
# CONFIGURACIÓN GENERAL
# ============================================

# Entorno: development, production, test
NODE_ENV=production

# Puerto de la API
PORT=3001

# ============================================
# BASE DE DATOS
# ============================================
# Configuración individual de base de datos
DB_HOST=localhost
DB_PORT=1433
DB_NAME=DB_Transparencia_Fiscal
DB_USER=sa
DB_PASSWORD=YourPassword123
DB_ENCRYPT=false
DB_TRUST_SERVER_CERTIFICATE=true

# DATABASE_URL se construye automáticamente en database.config.ts a partir de las variables anteriores
# Si necesitas definir DATABASE_URL manualmente, descomenta la línea siguiente:
# DATABASE_URL="sqlserver://localhost:1433;database=DB_Transparencia_Fiscal;user=sa;password=YourPassword123;encrypt=false;trustServerCertificate=true"

# ============================================
# AUTENTICACIÓN JWT
# ============================================

# Secret para firmar tokens JWT (DEBE SER SEGURO EN PRODUCCIÓN)
# Genera uno con: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=30m

# ============================================
# CORS (Cross-Origin Resource Sharing)
# ============================================

# Orígenes permitidos (separados por coma)
CORS_ORIGIN=http://localhost:3000,http://localhost:3001,http://192.168.50.18:3001,http://192.168.50.18:3000

# ============================================
# LOGGING
# ============================================

# Nivel de logging: error, warn, info, debug
LOG_LEVEL=debug

# ============================================
# CONFIGURACIÓN DE EMAIL
# ============================================

MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=tu_correo@gmail.com
MAIL_PASSWORD=tu_contraseña_o_app_password
MAIL_FROM=transparencia.fiscal@morelos.gob.mx
MAIL_REPLY_TO=no-reply@morelos.gob.mx

# ============================================
# CONFIGURACIÓN DE SUBIDA DE ARCHIVOS
# ============================================

UPLOAD_DIR="./uploads"
MAX_FILE_SIZE="52428800" # 50MB
ALLOWED_EXTENSIONS="pdf,doc,docx,xls,xlsx,ppt,pptx,txt,csv,json,xml,zip,rar,7z"
AUTO_CREATE_DIRECTORIES="true"
# URL base de la API para URLs de archivos estáticos (default: http://localhost:3001)
# API_BASE_URL=http://localhost:3001

# ============================================
# CONFIGURACIONES ADICIONALES
# ============================================

# URL de la aplicación (para enlaces en emails)
APP_URL=http://localhost:3000

# Emails para notificaciones internas (separados por coma)
MAIL_INTERNAL_NOTIFICATIONS=admin@morelos.gob.mx,transparencia@morelos.gob.mx

# ============================================
# INSTRUCCIONES DE USO
# ============================================

# 1. DESARROLLO LOCAL:
#    - Copia este archivo como .env.development
#    - Configura DATABASE_URL con tus credenciales locales
#    - Ejecuta: npm run start:dev

# 2. PRODUCCIÓN:
#    - Copia este archivo como .env.production
#    - Configura variables con valores reales de producción
#    - Usa: npm run build && npm run start:prod

# 3. PRUEBAS:
#    - Copia este archivo como .env.test
#    - Configura una base de datos separada para pruebas
#    - Usa: npm run test

# 4. SEGURIDAD:
#    - NUNCA commits archivos .env reales a Git
#    - Usa diferentes JWT_SECRET para cada entorno
#    - En producción, usa un servicio de gestión de secretos
